# Deprecated: wrangler.worker.toml
# This repository now uses wrangler.toml as the canonical Wrangler configuration.
# The original configuration has been moved to wrangler.worker.toml.bak
# to avoid multiple active Wrangler configs. If you need the old config,
# inspect wrangler.worker.toml.bak in this branch.

# NOTE: Keep this file minimal to prevent accidental double-config during deploys.
